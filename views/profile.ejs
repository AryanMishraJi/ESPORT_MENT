<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Profile</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-100">
    <div class="max-w-3xl mx-auto my-10 bg-white shadow-md rounded-lg overflow-hidden">
        <div class="flex items-center justify-center bg-blue-600 p-6">
            <div class="text-center">
                <h1 class="text-3xl font-semibold text-white mb-2">User Profile</h1>
                <p class="text-blue-200">Welcome, <%= name %>!</p>
            </div>
        </div>

        <div class="p-8">
            <div class="mb-6">
                <h2 class="text-xl font-semibold text-gray-700 mb-2">Personal Information</h2>
                <ul class="text-gray-600">
                    <li class="mb-2"><span class="font-bold">Name:</span> <%= name %></li>
                    <li class="mb-2"><span class="font-bold">Registration Number:</span> <%= registrationNumber %></li>
                    <li class="mb-2"><span class="font-bold">Phone:</span> <%= phone %></li>
                    <li class="mb-2"><span class="font-bold">Email:</span> <%= email %></li>
                </ul>
            </div>
        </div>

        <div class="bg-gray-50 p-4 text-center">
            <a href="/" class="text-blue-600 hover:text-blue-800 font-medium">Back to Home</a>
            <button onclick="logout()" class="text-red-600 hover:text-red-800 font-medium ml-4">Logout</button>
        </div>
    </div>

    <script>
        function logout() {
            fetch('/logout', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json'
                }
            }).then(response => {
                if (response.ok) {
                    window.location.href = '/'; 
                }
            });
        }
    </script>
</body>

</html>
